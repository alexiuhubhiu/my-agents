"""
my_agents.core — 记忆底层（通用层）
====================================
领域无关的记忆底座：会话/情节/语义/核心记忆 + 通用状态 + 进化框架。
不 import 任何 personas.* 内容，作为可插拔底座向上层服务。

红线（同 ENGINE.md）：
- core 绝不 import personas / 任何领域常量
- core 只依赖本包内模块
"""

__version__ = "1.0.0"
